# AI 시설물 안전점검 시스템 — 앱 아키텍처 설계

> 가천 AI 부트캠프 현장미러형 9번 / 출제: 씨엠티정보통신 / 1인 개발(이호민) · 최신화 7/28
> 본 문서는 **현재 가동 중인 시스템 구조**를 반영한다(초기 설계본에서 전면 갱신).

---

## 1. 설계 원칙

- **점수는 Rule 엔진(코드)이 결정** — YOLO·RAG·LLM은 "재료"만 제공하고 위험도 산정은 코드가 결정론적으로 수행.
- **근거는 RAG 검색이 공급** — 안전기준 근거 문장은 검색 결과에서만 인용, LLM이 지어내지 않음.
- **서술만 LLM이 담당** — 보고서 문장·질문·ROI 제안만 LLM. "재계산 금지·지어내지 말 것"을 프롬프트에 명시 → **환각 구조적 차단**.
- **6종 복합 결함 진단** — 균열·박리/박락·백태/누수·철근노출·강재손상·도장손상(공식 과제 9번 스펙).
- **mm 절대측정 안 함** — 픽셀 상대값(길이비·폭 px)으로 처리, 정밀 측정은 전문 점검 몫.
- **전 구간 방어적 폴백** — 모델·API·인덱스가 없어도 앱이 죽지 않도록 각 단계가 안전 폴백.
- **비전문가·모바일 우선** — 스마트폰 사진 한 장으로 근접 자가진단.

---

## 2. 기술 스택

| 계층 | 선택 | 이유 |
|---|---|---|
| UI/앱 | Streamlit (모바일 웹) | 사진 촬영/업로드·결과·근거·보고서를 단일 앱으로, 빠른 프로토타이핑 |
| 결함 탐지(현 가동) | YOLOv8s **bbox 2모델 하이브리드**(crack 전용 + defect6) + 640 타일 추론 | 고해상도 균열선 보존, crack 전용 모델이 수직 균열 오분류 교정 |
| 결함 탐지(고도화) | YOLOv8s-seg (71769) | 선형 균열 마스크 표현 실증(mask mAP50 72.2%), 재학습 후 정식 활성 |
| 사진 게이트·ROI | Claude Vision | 부적합 사진 사전 컷 + 육안 메타, 원거리/배경 시 ROI 2단계 탐지(오탐 감소) |
| 형태 분석 | OpenCV (adaptive threshold + Canny + skeletonize) | 균열 길이·폭·개수·중심선 정량화(얇은 균열 라인 보존) |
| 위험도 | Rule Engine (순수 파이썬) | 신뢰도·개수·길이·복합·비전관찰 기반 점수→등급. 설명·재현 가능 |
| 지식검색 | RAG: ChromaDB(코사인) + Upstage Solar 임베딩(비대칭) | 한국어 안전기준 근거 검색 |
| 보고서 | Claude → Solar → 목업 폴백 | 7섹션 서술 생성, 제공자 자동 선택 |
| 대화 수집(고도화·OFF) | LangGraph 에이전트 / 룰 기반 폴백 | 시설물명·위치 등 기본현황을 대화로 수집 |
| 배포 | GCE + Docker Compose + Cloudflare Tunnel + GitHub Actions CD | 무료 HTTPS 터널로 폰 접속, main push 시 자동 재배포 |
| 대용량 파일 | Git LFS | 모델(*.pt)·원문 PDF 버전관리 |

---

## 3. 시스템 아키텍처 (전체 파이프라인)

```
[1] 사진 업로드 (Streamlit, 모바일)
        ▼
[2] 비전 트리아지 (triage.py · Claude Vision)
        │  적합성 게이트(원거리·흐림·비대상 → 재촬영/반려) + 육안 메타(부위·재질·양상·관찰결함)
        ▼
[2.5] ROI 2단계 탐지 (roi_triage.py · Claude Vision, 기본 ON)
        │  원거리/배경 포함 시: 결함 영역 ROI만 골라 크롭 → YOLO → 좌표 원복·병합
        │  근접 촬영: full_image_ok → 전체 이미지 그대로 (API 실패 시에도 전체 탐지 폴백)
        ▼
[3] 결함 탐지 (detector.py) — bbox 2모델 하이브리드
        │  crack 전용 모델(균열) + defect6 모델(면적 결함 5종), 640 타일 슬라이스 추론
        │  ※ seg 하이브리드(segmenter.py)는 고도화·기본 OFF
        ▼
[3.5] 후처리 (postprocess.py)
        │  이음새(줄눈) 직선 오탐 필터 · 겹친 균열 박스 병합 · 물리적 균열 개수 보정
        ▼
[4] 형태 특징 (features.py · OpenCV)
        │  adaptive threshold + Canny + skeletonize → 길이·폭·개수·중심선
        ▼
[5] Rule 위험도 (rules.py)          ‖   [6] RAG 근거 검색 (rag.py + embedder.py)
        │  신뢰도·개수·길이·복합·         │  결함/등급 → 쿼리 → Solar 쿼리 임베딩
        │  비전관찰 가점 → 점수→등급       │  → ChromaDB top-k → 근거 문장+출처
        └───────────────┬──────────────────┘
                        ▼
[7] 보고서 생성 (report.py) — Claude → Solar → 목업
        │  탐지 수치+등급(코드)+근거(RAG)+비전 메타 → 7섹션 서술(2·3·4는 LLM, 1·5·6은 조립)
        ▼
[8] 결과 화면 (app.py) — 등급 카드 · 탐지 오버레이 · 지표 · RAG 근거 · 보고서 · .md/PDF 다운로드

(선택) 멀티턴 대화 정보수집 (multiturn.py / v3_langgraph, 기본 OFF) — 보고서 기본현황을 대화로 수집
```

핵심: **[5] Rule과 [6] RAG는 병렬** — Rule이 점수를, RAG가 근거를 담당. 오케스트레이터(`orchestrator.py`)가 전 단계를 `AgentState`로 이어 관리(LangGraph 이식 용이).

---

## 4. 모듈 / 폴더 구조

```
AI_crack_detection/
├─ app.py / app_basic.py     # Streamlit 진입점(대화 에이전트 포함/미포함)
├─ pipeline/
│   ├─ orchestrator.py       # 전체 흐름·AgentState 관리
│   ├─ triage.py             # [2] 비전 트리아지(게이트+메타)
│   ├─ roi_triage.py         # [2.5] ROI 2단계 탐지(기본 ON)
│   ├─ detector.py           # [3] YOLO 타일 추론·bbox 하이브리드
│   ├─ segmenter.py          # [3] seg 하이브리드(고도화·OFF)
│   ├─ postprocess.py        # [3.5] 이음새 필터·박스 병합·개수 보정
│   ├─ features.py           # [4] OpenCV 형태 분석(스켈레톤)
│   ├─ rules.py              # [5] Rule Engine → score/grade
│   ├─ rag.py / embedder.py  # [6] ChromaDB 검색 + Solar 임베딩
│   ├─ report.py             # [7] 보고서(Claude→Solar→목업)
│   ├─ pdf_export.py         # 보고서 PDF 내보내기
│   └─ multiturn.py          # (선택) 룰 기반 대화 수집
├─ v3_langgraph/             # (선택) LangGraph 대화 에이전트(기본 OFF)
├─ knowledge/                # 안전기준 원문·요약·ChromaDB 인덱스
├─ models/                   # LFS: crack/ defect6/ seg 가중치(*.pt)
├─ tools/deid_blur.py        # 현장 이미지 개인정보 비식별화 전처리
├─ config.py / schemas.py    # 상수·임계값 / 단계 간 데이터클래스
├─ Dockerfile / docker-compose*.yml / entrypoint.sh
└─ .github/workflows/cd.yml  # main push → LFS pull → 빌드·재배포·스모크
```

코딩 컨벤션: 주석·docstring 한국어 / 함수·변수명 영어 / 경로·상수 `config.py` 집약 / 각 노드 방어적 폴백.

---

## 5. 단계별 입출력 스키마 (schemas.py)

```python
AgentState = {           # 세션 전체 상태(캐시)
  stage, image_hash, triage, detect, features, risk, rag, report,
  crack_mask,            # seg 마스크(옵션)
  rois,                  # ROI 2단계 탐지 영역(옵션·시각화)
}
TriageResult  = {verdict, ok, message, provider, blur_score, meta{부위·재질·양상·관찰결함}}
Detection     = {box[x1,y1,x2,y2], conf, cls, label}   # label: crack/spalling/efflorescence/rebar_exposure/steel_defect/paint_damage
DetectResult  = {detections[], image_size[W,H]}
CrackFeatures = {crack_count, max_length_ratio, avg_width_px, max_confidence, defects{label:{count,max_conf}}}
RiskResult    = {score, grade(정상/주의/위험/긴급), contributions[]}
Evidence      = {text, source, score, title, doc_type, publisher, url, defect}
RagResult     = {evidences[]}
Report        = 7섹션(기본현황·점검결과·안전등급·종합의견·판단근거(RAG)·유의사항·권고사항)
```

---

## 6. Rule Engine 설계 (rules.py)

점수는 코드가 결정론적으로 합산하고 GRADE_BINS로 등급을 매긴다. **가중치·임계값의 완전한 표는 별도 산출물 `위험도_산정_규칙.md` 참조.**

- 등급: 0–39 정상 / 40–69 주의 / 70–89 위험 / 90+ 긴급
- 균열 채널: 신뢰도 ≥0.10 +30 / ≥0.05 +15, 개수 ≥5 +25·≥3 +15, 길이 ≥15% +15
- 면적 결함(복합): 철근노출+30·강재+25·박락+20·누수+10·도장+5(결함별 신뢰도 하한 적용)
- 복합 가점: 동일 결함 3+ +5, 서로 다른 결함 2종+ +10
- **비전 관찰 가점**: YOLO 저신뢰도(<0.05)라도 비전이 결함 확인 시 +20(복합 유형당 +10, 최대 +20)
- 모든 가점은 `contributions`에 근거를 남겨 "왜 이 등급인지" 화면에 표시(설명가능성)

> 신뢰도 캘리브레이션 — 임계값은 본 모델의 실제 신뢰도 분포에 최적화해 설정하고, 결함별 하한·형태·비전 검증으로 오탐을 억제한다.

---

## 7. RAG 지식베이스 설계 (knowledge/)

**소스** — 현행 안전기준 17문서(원문 12건 아카이브): 시설물 안전점검·정밀안전진단 세부지침, 콘크리트구조 설계기준(허용균열폭 0.3mm), 공동주택 하자 판정기준, 균열보수 시방 등.

**파이프라인**
1. `build_index.py`: 원문 → 문단 청킹(98청크) → **Solar passage 임베딩** → ChromaDB(코사인) 구축.
2. 런타임 `rag.py`: 결함/등급 → 쿼리 생성 → **Solar query 임베딩**(비대칭) → top-k(3) 검색 → 근거 문장 + 출처(기준명·발행처·URL).
3. 매칭 임계값 0.30, 실측 7종 결함 쿼리 100% 통과(유사도 0.467~0.589).

핵심 원칙: **RAG는 점수를 매기지 않는다.** 근거 문장만 제공하고, 인용은 파일명이 아니라 기준명·출처로 한다.

---

## 8. 보고서 생성 설계 (report.py)

- 제공자 체인: **Claude(claude-sonnet-4-5) → Solar(solar-pro2) → 목업** 자동 선택(키·설정 따라).
- 입력: CrackFeatures + RiskResult + RagResult + 트리아지 메타.
- 원칙: 비전문가용 쉬운 서술, **근거는 RAG 인용·점수는 재계산 금지**, 과장 금지, "전문가 정밀점검 권고" 고정.
- 출력: 7섹션(기본현황·점검결과·안전등급·종합의견·판단근거·유의사항·권고사항), 서술 3섹션(2·3·4)만 LLM, 나머지는 코드/RAG 조립.
- 다운로드: Markdown + PDF(`pdf_export.py`, 한글 폰트).

---

## 9. 화면 구성 (app.py, Streamlit)

세로 흐름: ① 사진 촬영/업로드 → ② 진행 단계 표시(트리아지→ROI→탐지→특징→RAG→보고서) → ③ 탐지 오버레이(결함별 색상·범례, seg 시 마스크) → ④ 위험도 카드(등급 색상·점수·기여 내역) → ⑤ 정량 지표 + feature 요약 → ⑥ RAG 근거(기준명·출처) → ⑦ 보고서 초안 + **.md/PDF 다운로드**. (선택) 대화형 정보 보충 섹션.

---

## 10. 데이터 플로우 (요청→응답, 1회)

```
사진 1장
 → triage: 적합성 게이트 + 메타 (Claude Vision)
 → (원거리/배경) roi_triage: ROI 추출 → 크롭 (Claude Vision)
 → detector: bbox 2모델 하이브리드 타일 추론 → 박스
 → postprocess: 이음새 필터·병합·개수 보정
 → features: 스켈레톤 → count/length/width
 → (병렬) rules: score/grade  |  rag: 근거 top-k(Solar+Chroma)
 → report: Claude→Solar→목업 → 7섹션
 → Streamlit: 이미지+등급+지표+근거+보고서 → .md/PDF
```

무거운 결과(triage/detect/features)는 `AgentState`로 캐시해 Streamlit 재실행 시 재계산을 막는다. 외부 호출(Claude·Solar)은 모두 실패 시 폴백 → 오프라인/무키 환경에서도 앱이 동작.

---

## 11. 배포·운영

- **GCE VM + Docker Compose** — 2 컨테이너: 8501(복합·bbox 하이브리드, CD 자동배포) / 8502(균열 안전판, 동결·수동).
- **Cloudflare Tunnel** — 무료 HTTPS로 모바일 접속.
- **GitHub Actions CD** — main 머지 → SSH 접속 → `git-lfs pull`(모델) → 빌드 → 스모크 테스트 → 재배포. LFS 인증은 서버 SSH 배포키 + known_hosts 등록.
- **Git LFS** — 모델(*.pt)·원문 PDF.

---

## 12. 고도화·향후

- seg 하이브리드 정식 활성 — 도메인 다양성(현장 이미지 + AIHub 결합) 재학습 후.
- ROI 2단계 탐지 정밀화, 멀티턴 대화 에이전트 서버 배포(langgraph 의존성 추가).
- 현장 테스트셋 정량 평가(GT 라벨) 확대.
