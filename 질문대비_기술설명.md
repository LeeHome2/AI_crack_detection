# 질문 대비 기술 설명 문서 — AI 시설물 안전점검 시스템 (프로젝트 9번)

> Q&A에서 나올 만한 기술 질문에 바로 답하기 위한 정리. 모든 수치·모델명·파라미터는 실제 코드(config.py·pipeline/·knowledge/) 기준.

---

## 0. 전체 스택 한 줄 요약

사진 1장 → **비전 트리아지(Claude)** → **ROI 2단계(Claude)** → **YOLOv8 하이브리드 탐지** → **OpenCV 형태 특징** → **규칙 엔진 위험도** ∥ **RAG 근거 검색(Solar+ChromaDB)** → **보고서 생성(LLM)** → **대화형 보충(Claude)**. 오케스트레이터(`orchestrator.analyze`)가 전 단계를 하나의 `AgentState`로 관리.

---

## 1. 용어 사전 (Glossary)

- **트리아지(Triage)**: 의료의 '중증도 분류'에서 온 말. 여기서는 본격 분석 전에 **사진이 분석에 적합한지 걸러내는 1차 게이트**. 흐림·원거리·비대상이면 재촬영/반려.
- **ROI (Region of Interest)**: 관심영역. 사진 전체가 아니라 **결함이 있을 법한 영역만** 잘라서 분석하는 대상 좌표.
- **bbox (Bounding Box)**: 결함을 감싸는 사각 박스. YOLO의 기본 출력 형태.
- **세그멘테이션(Segmentation)**: 박스가 아니라 **픽셀 단위 마스크**로 결함 모양을 따내는 방식. 선형 균열에 적합.
- **타일링(Tiling)**: 큰 원본 이미지를 격자(타일)로 쪼개 각각 추론 후 합치는 기법. 축소 없이 **미세 균열 신호 보존**.
- **NMS (Non-Maximum Suppression)**: 같은 대상에 겹쳐 잡힌 박스 중 신뢰도 높은 것만 남기고 중복 제거. IoU 기준.
- **IoU (Intersection over Union)**: 두 박스의 겹침 비율. NMS·병합의 판단 기준(본 시스템 ROI 병합 IoU=0.5).
- **mAP50**: 객체탐지 표준 지표. IoU 0.5 기준 평균 정밀도(mean Average Precision). 값이 클수록 탐지 성능 우수.
- **하이브리드(Hybrid)**: 균열 전용 모델 + 복합 6종 모델 **두 개를 함께** 써서 서로의 약점을 보완하는 구성.
- **RAG (Retrieval-Augmented Generation)**: 검색으로 찾아온 **실제 근거 문서**를 바탕으로 답을 만드는 방식. 환각(할루시네이션) 방지.
- **임베딩(Embedding)**: 문장을 의미 벡터로 변환. 벡터 간 거리로 유사도 계산.
- **청킹(Chunking)**: 긴 문서를 검색 단위(청크)로 잘게 나누는 것.
- **폴백(Fallback)**: API·모델이 없거나 실패해도 서비스가 죽지 않게 대체 경로(휴리스틱/목업)로 동작하는 것.

---

## 2. AI 모델 구성 (모델명·역할)

| 파트 | 모델 | 역할 | 코드 |
|---|---|---|---|
| 비전 트리아지 | **Claude (claude-sonnet-4-5)** | 적합성 판정 + 육안 소견 추출 | `triage.py`, `VISION_MODEL` |
| ROI 2단계 | **Claude (claude-sonnet-4-5)** | 결함 관심영역 좌표 반환 | `roi_triage.py` |
| 결함 탐지 | **YOLOv8s** (crack 전용 + defect6 6종) | bbox 탐지 | `detector.py` |
| 세그멘테이션(고도화) | **YOLOv8s-seg** | 균열 마스크(현재 기본 OFF) | `segmenter.py` |
| 근거 임베딩 | **Solar (Upstage)** — `solar-embedding-1-large-passage/query` (4096차원) | 문서·쿼리 임베딩 | `embedder.py` |
| 보고서 서술 | **Claude → Solar(`solar-pro2`) → 목업** | 서술 섹션 생성 | `report.py` |
| 대화형 보충 | **Claude** | 부족 정보 대화 수집 | `chat_agent.py` |

- **제공자 폴백 체인**: 보고서는 `claude`(ANTHROPIC 키) → `solar`(UPSTAGE 키) → `mock` 순으로 자동 선택. 임베딩은 `solar` → 로컬 `BGE-m3` → `mock`.
- 비전(트리아지·ROI)은 보고서 LLM과 **같은 Claude 키·모델을 재사용**.

---

## 3. 데이터 (AI Hub 특징 · 학습 장수)

### 3-1. 공개 데이터(AI Hub) 3종 — 균열
| 키 | 이름 | 라벨 | 해상도 | 특징 |
|---|---|---|---|---|
| **162** | 건물 균열 탐지드론 개발용 이미지 | COCO **bbox** | 2560×1440 TIFF | 드론 촬영 콘크리트 균열(ConcreteCrack) |
| **567** | 서울시 노후 주택 균열 데이터 | **Polygon** | 1440×1080 JPG | 미세/보통/심한 3단계, 다세대·단독·아파트 등 주거 위주 |
| **71769** | SOC 시설물 균열패턴 이미지 | **Polyline** | 1920×1080 JPG | 도로터널 등 SOC, 망상/선형 균열 중심선 — **세그멘테이션 주력 학습셋** |

### 3-2. 복합 결함 6종(defect6) 데이터
- 클래스 6종: **crack(균열) · spalling(박리) · efflorescence(백태·누수) · rebar_exposure(철근노출) · steel_defect(강재손상) · paint_damage(도장손상)**
- 콘크리트 복합 열화를 다중클래스로 학습한 세트.

### 3-3. 학습 장수 (로컬 학습 기록)
| 모델 | 데이터셋 | Train / Val | Epoch | 비고 |
|---|---|---|---|---|
| 균열 단일(현 가동) | dataset_tiled | **2,014 / 524장** | 50 | `train_tiled_v1` (best.pt) |
| 복합 6종 | dataset_6class | **21,788 / 4,155장** | 100 | `defect6` |
| 세그멘테이션 | dataset_seg_tiled(71769) | 71,769 데이터셋 기반 | 100 | 고도화·현재 OFF |

- **입력 크기**: 640px 타일 · GPU: 데스크탑 RTX 3070 Ti.
- 총 규모: 균열 단일 ~3,900장(원본)/~2,500장(타일), 6클래스 ~26,000장, 세그 71,769 기반.

### 3-4. 현장 검증 데이터 (CP4)
- **직접 촬영한 현장 사진 200장 이상**. 개인정보(번호판·얼굴) 자동 비식별화 + EXIF·규격 정규화 전처리 후 **서비스 전 과정 검증**에 사용. (학습이 아니라 검증용)

---

## 4. 탐지 파이프라인 (YOLO 하이브리드 · 타일링)

### 4-1. 타일 슬라이싱 추론 — "640만 보는 게 아니다"
- 모델 추론 크기는 **640×640**(학습도 640). 하지만 **원본을 640으로 축소해 한 번 보는 게 아니라**, 원본을 **640 타일로 슬라이딩하며 여러 번 추론 후 병합**한다.
- 파라미터(`config`): `TILE=640`, `OVERLAP=0.2` → **stride = 640×(1−0.2) = 512**. 즉 **512px 간격(=128px 겹침)으로 640×640 타일**을 잘라(`img[ty:ty+640, tx:tx+640]`) 각각 `model.predict(tile, conf=0.05)`.
- 마지막 타일은 이미지 끝(`total−640`)에 정렬 → 가장자리도 누락 없이 커버. 각 타일 박스는 `+tx,+ty`로 **원본 좌표로 역변환**.
- 효과: 축소 없이 **원본 해상도의 미세 균열 신호 보존**. (예외: 사진이 가로·세로 모두 640 이하로 작으면 타일 1개=전체 이미지를 640으로 레터박싱해 추론.)

### 4-2. bbox 2모델 하이브리드 — 어떻게 작동하나 (★자주 나오는 질문)
- **조건부 순차 아님.** 두 모델을 **같은 이미지에 각각 독립적으로** 돌린 뒤 결과를 합친다(코드 실행은 crack→defect6 순이지만 서로 의존 없음).
- **역할 분담(클래스로 분리)**:
  - `crack 전용 모델` → **균열(crack)만** 담당(균열 특화 학습이라 더 정확).
  - `defect6 모델` → 6종을 탐지하되 코드에서 **`exclude_labels={"crack"}`로 crack 출력을 버림** → 나머지 5종(박리·백태누수·철근노출·강재·도장)만 기여.
  - → 같은 균열을 두 모델이 중복으로 내는 것을 막고, 균열은 잘하는 모델이 맡음.
- **각자 전체 이미지 타일 추론**(4-1) → 두 결과 리스트 결합(`raw.extend`).
- **라벨별 NMS 병합**(IoU=`config.IOU_MERGE`): 같은 라벨끼리 겹친 박스만 중복 제거 → 최종 `DetectResult`.
- **왜?** defect6 단일 모델은 **수직 균열을 다른 결함으로 오분류**하는 문제가 있어, 균열을 전용 모델로 분리해 교정.

### 4-3. 후처리 & 성능
- **후처리**: 이음새(타일 줄눈·패널 경계) 오탐 필터(`postprocess.filter_seams`), 겹치는 균열 박스 병합(`merge_overlapping_boxes`), 물리적 균열 개수 보정(`physical_crack_count` — 한 줄이 여러 박스여도 1개로 카운트).
- **성능**: 6종 복합 mAP50 **14.3%**(50% 타일 모델), 클래스별 AP50는 철근노출 28%·균열 19%·강재 11%·백태 10%·도장 7%·박리 5%로 편차 있음. → 그래서 클래스별 신뢰도 하한을 다르게 둠(강한 클래스는 낮춰 탐지, 약한 클래스는 높여 오탐 억제).

---

## 5. OpenCV 형태 특징 (`features.extract`)

탐지된 박스 내부에서 균열의 **길이·폭·개수·중심선**을 정량화.

- **균열 마스크 추출(`_crack_mask`)** — 두 방법 하이브리드:
  1. **Adaptive Threshold**(Gaussian, blockSize=25, C=10) — 거친 텍스처 표면에 강함.
  2. **Canny 엣지**(50,150) — 깨끗한 벽면의 얇은 균열 보완(1번 결과 부족 시).
- **연결요소 필터링**: 세장비(가늘고 긴, elongation ≥ 3.0)·채움비(≤0.6) 조건으로 **균열 형태만** 남기고 텍스처 노이즈 제거.
- **스켈레톤(중심선) 추출**: `skimage.skeletonize`로 중심선 픽셀 수 = 길이. **폭 = 균열 픽셀 수 ÷ 길이**. 길이는 crop 대각선으로 상한 클리핑(노이즈 방어).
- mm 절대측정은 참조객체가 없어 불가 → **상대값(픽셀·이미지 대각선 비율)**으로 표기.

---

## 6. ROI 2단계 탐지 (`roi_triage.detect_with_roi`)

1. **Claude Vision**에 이미지를 주고 결함 관심영역 좌표를 **정규화 좌표(0~1의 x1,y1,x2,y2)** + `full_image_ok`로 반환받음.
2. `full_image_ok`(근접 사진)이면 전체 이미지 탐지. 아니면 각 ROI를 **크롭 → YOLO 탐지 → 원본 좌표로 역변환 → NMS(IoU 0.5) 병합**.
3. **폴백**: Vision 키 없음/JSON 파싱 실패/API 오류 시 `full_image_ok=True`로 전체 이미지 탐지 → 서비스 무중단.
- 효과: 원거리·배경 포함 예시에서 배경 오탐 **균열 11→8, 박리 32→27** 감소.

---

## 7. RAG (문서 적재 · 청킹 · 임베딩 · 검색)

### 7-1. 문서 적재 & 청킹 (`knowledge/build_index.py`)
- **원본**: `knowledge/raw/`의 안전기준 **txt 17개**(콘크리트 허용균열폭, 상태평가등급, 균열 보수공법, 철근노출·박리·백태·강재·도장 기준, 복합결함 종합판정, 비전문가 FAQ 등).
- **@meta 프런트매터** 파싱: 각 문서 상단 `@meta ... @endmeta`에서 **기준명·문서종류·발행처·출처URL·결함유형**을 뽑아 모든 청크 메타에 부착 → 인용 시 파일명이 아니라 **실제 출처(기준명·발행처·URL)** 제시.
- 하단 `출처:` 블록은 검색 본문에서 분리(메타로만 보관, 검색 오염 방지).
- **청킹 방식**: **문단(빈 줄) 우선** 분할로 원문 발췌 보존. 문단이 400자를 넘으면 **size=400 / overlap=80** 슬라이딩으로 세분.
- 결과: 17문서 → **98청크** 인덱싱.

### 7-2. 임베딩 & 저장소
- **임베딩 모델**: Upstage **Solar 비대칭 임베딩** — 문서는 `solar-embedding-1-large-passage`, 쿼리는 `solar-embedding-1-large-query`(같은 4096차원 벡터공간이라 상호 비교 가능). 로컬 폴백은 `BAAI/bge-m3`.
- **벡터DB**: **ChromaDB**(PersistentClient), 컬렉션 `safety_standards`, **거리 메트릭 = 코사인**(`hnsw:space=cosine`).
- KB_VERSION 마커로 배포 시 인덱스 자동 재빌드(옛 인덱스 잔존 방지).

### 7-3. 검색 & 쿼리 생성 — ★자주 헷갈리는 포인트
- **쿼리는 LLM이 만들지 않습니다.** 사용자 입력도 아닙니다. **탐지 결과에서 규칙 기반으로 자동 생성**(`rag._build_query`): 탐지된 결함 종류·균열 폭 유무·위험 등급을 조합.
  - 예: `"시설물 콘크리트 안전점검 기준 + 균열 + 균열 폭 0.3mm 허용 기준 조치 + (위험/긴급이면) 정밀안전진단 긴급보수"`
- 이렇게 만든 쿼리 문자열을 **Solar query 임베딩** → ChromaDB에서 **top_k=3** 검색.
- **임계값 0.30**(코사인) 이상만 '강한 근거'로 채택. 실측 대표 쿼리 7종 전부 통과(유사도 0.47~0.59).
- 즉 **"쿼리 생성 = 규칙, 임베딩·검색 = Solar+ChromaDB, 서술 = LLM"**으로 역할이 분리됨.

### 7-4. RAG 검증 방법 — 7종 스모크 테스트 (`knowledge/rag_smoke_test.py`)
- **결함 6종 + 복합 1종 = 7개 대표 쿼리**를 손으로 작성해, 각 쿼리가 **해당 결함의 안전기준 문서를 top-3 안에 검색해오는지** 확인하는 리트리벌(검색 정확도) 테스트.
- 7개 쿼리 → 기대 문서(결함태그):
  1. "철근이 드러나고 녹이 슬어 콘크리트가 떨어져 나감" → `rebar_exposure`
  2. "콘크리트 표면이 들뜨고 박락되어 떨어짐" → `spalling`
  3. "벽면에 흰색 백태가 생기고 물이 새어 누수 흔적" → `efflorescence`
  4. "강재 부재가 부식되어 단면이 손실됨" → `steel_defect`
  5. "강교 도장이 벗겨지고 방식 코팅 손상" → `paint_damage`
  6. "균열 폭 0.3mm 초과 허용기준 보수" → `crack`
  7. "여러 결함이 함께 발생한 복합 열화 종합판정" → `general`
- **판정**: 각 쿼리를 Solar 쿼리 임베딩 → ChromaDB top-3 검색 → 뽑힌 청크의 `defect` 메타태그에 **기대 결함이 포함되면 통과(✅)**. 유사도 = `1 − 코사인거리`.
- **결과**: **7/7 통과, 유사도 0.47~0.59**(전부 임계값 0.30 위).
- ★구분: 이 7개는 **검증용 자연어 쿼리**이고, 실서비스 런타임 쿼리는 `_build_query`(탐지 결과 기반 규칙 조립)로 별개. 스모크 테스트는 "결함 유형별로 올바른 문서를 찾아오는지"를 확인하는 용도.

---

## 8. 규칙 엔진 위험도 공식 (`rules.evaluate`)

- **균열(별도 채널)**: 최고 신뢰도 ≥ 0.10 → **+30**, ≥ 0.05 → **+15**. 긴 균열 **+15**, 다수 **+15**. (복합 가중표에서 crack=0으로 중복 방지)
- **복합 결함 가중**: 철근노출 **+30** · 강재 **+25** · 박리 **+20** · 백태누수 **+10** · 도장 **+5**.
  - 결함별 **신뢰도 하한**(AP50 성능 프록시): 철근노출 0.25·강재 0.35·박리 0.45·백태 0.35·도장 0.45·균열 0.20 — 강한 클래스는 낮춰 탐지, 약한 클래스는 높여 오탐 억제.
- **복합 2종+ 동시 +10**, 동일 결함 3개↑ **+5**.
- **비전 관찰 가점**: YOLO 신뢰도 < 0.05라도 비전이 결함 확인 시 **+20**(균열/기타).
- **RAG 강근거(≥0.30) +20**.
- **등급 구간**: 0–39 정상 · 40–69 주의 · 70–89 위험 · 90+ 긴급.
- 핵심: 점수는 **LLM이 아니라 코드가 결정론적으로 계산** → 기여 내역을 모두 표시(설명·재현 가능).

---

## 9. 오케스트레이션 노드 분기 기준 (`orchestrator.analyze`)

1. **[게이트] 트리아지**: `triage.ok`가 False면 무거운 YOLO를 돌리지 않고 조기 반환.
   - `verdict != "not_crack"` → `NEEDS_RETAKE`(재촬영), `== "not_crack"` → `REJECTED`(반려).
   - 트리아지 내부: **흐림 선검사**(Laplacian blur < 60이면 API 전에 즉시 컷) → 통과 시 Claude Vision 판정 → 실패 시 휴리스틱 폴백(흐림 아니면 통과).
2. **[분기] ROI 사용 여부**: `ROI_TRIAGE_ENABLED` & `roi_triage.is_enabled()`면 ROI 2단계, 아니면 하이브리드/단일 탐지. `detector.is_hybrid_ready()`로 하이브리드 가능 여부 판단.
3. **[후처리]** 이음새 오탐 필터 → 겹침 박스 병합(옵션).
4. **[분기] seg 하이브리드**: `SEG_HYBRID_ENABLED` & `segmenter.is_ready()`일 때만 균열을 seg 마스크로 대체(현재 기본 OFF, 예외 시 bbox 폴백).
5. **[분기] 특징 소스**: seg 마스크가 있으면 **마스크에서 직접** 균열 특징 계산, 없으면 **OpenCV bbox 경로** + 물리적 개수 보정.
6. **[2패스] 위험도**: RAG 전에 1차 `rules.evaluate` → 그 등급으로 RAG 쿼리 생성·검색 → RAG·비전 반영해 **최종 `rules.evaluate`** 재계산.
7. **[옵션] 보고서**: `skip_report`면 생성 생략(빠른 테스트).

---

## 10. 배포 · 형상관리 (요약)

- **배포**: GCE + Docker Compose(복합/크랙 2컨테이너 이중화) + Cloudflare Tunnel(HTTPS, 폰 접속) + GitHub Actions CD(main 머지 시 모델 LFS pull·자동 재배포·스모크).
- **형상관리**: Git 브랜치·커밋, Git LFS(모델 가중치 best.pt), 배포 전 스모크 테스트, 산출물 10종 문서화, 커밋=배포 시점 재현성.

---

## 11. 예상 질문 & 방어 답변 (빠른 참고)

- **Q. 쿼리 생성에 LLM 쓰나요?** → 아니요. 탐지 결과(결함·폭·등급)에서 **규칙 기반으로 문자열을 조립**하고, 그걸 Solar query 임베딩으로 검색합니다. LLM은 서술에만 사용.
- **Q. 임베딩 모델은?** → Upstage Solar 비대칭 임베딩(passage/query, 4096차원). 오프라인 폴백은 BGE-m3.
- **Q. 청킹 기준은?** → 문단 우선, 400자 초과 시 size 400·overlap 80 슬라이딩. 17문서 → 98청크. 코사인 거리, top_k 3, 임계값 0.30.
- **Q. 지금 YOLO는 640×640만 보나요?** → 모델 추론은 타일당 640이지만, 원본을 축소하는 게 아니라 **stride 512·128px 겹침으로 640 타일을 슬라이딩**해 전체를 훑고 병합합니다. 그래서 큰 사진에서도 원본 해상도 신호가 보존됩니다.
- **Q. 두 bbox 모델 하이브리드는 순차 실행인가요?** → 조건부 순차가 아니라 **두 모델이 같은 이미지를 각자 독립 탐지 후 병합**합니다. 균열은 crack 전용 모델, 나머지 5종은 defect6(그 crack 출력은 버림)가 담당하고, 라벨별 NMS로 합칩니다.
- **Q. RAG는 어떻게 검증했나요?** → 결함 6종+복합 1종 = 7개 자연어 쿼리로, 각 결함의 안전기준 문서가 top-3에 검색되는지 확인(7/7 통과, 유사도 0.47~0.59). 실서비스 쿼리(`_build_query`)와는 별개인 스모크 테스트.
- **Q. 세그멘테이션은 지금 켜져 있나요?** → 아니요(기본 OFF). 마스크 mAP50 72.2%로 실증 완료한 고도화 트랙. 현재는 bbox 하이브리드로 서비스.
- **Q. mm 폭은 왜 상대값인가요?** → 참조객체 없는 단일 사진으론 절대 mm 확정이 불가. 픽셀·대각선 비율의 상대값으로 표기하고, 라벨 데이터 확보 후 구간 추정을 로드맵으로 둠.
- **Q. API가 죽으면?** → 트리아지·ROI·보고서·임베딩 모두 폴백(휴리스틱/목업/BGE)로 무중단 동작.
- **Q. 왜 탐지 mAP가 낮아 보이나요?** → 스크리닝 도구라 '이상 유무 + 시각 품질 + 근거 보고서'가 목적. 타일링·하이브리드·ROI·세그(고도화)로 층층이 정교화했고, 최종 판단은 전문 점검으로 연결.

---

*근거 파일: `config.py` · `pipeline/{triage,roi_triage,detector,features,rules,rag,embedder,orchestrator}.py` · `knowledge/build_index.py` · `docs_DATASETS_INFO.md`*
