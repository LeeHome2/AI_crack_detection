# LLM 프롬프트·모델 정리 — AI 시설물 안전점검 시스템 (프로젝트 9번)

> 1인 개발 이호민 · 최신화 7/28
> 시스템에서 LLM/임베딩이 실제로 호출되는 지점은 **3곳**이다. 각 지점의 목적·모델·파라미터·프롬프트 전문·안전장치를 정리한다.
> 설계 철학: **점수는 코드(Rule)가, 근거는 검색(RAG)이, 서술만 LLM이 담당** → AI가 근거 없이 지어내지 못하게 구조적으로 차단.

---

## 0. LLM 호출 지점 개요

| # | 지점 | 파일 | 모델(기본) | 역할 |
|---|---|---|---|---|
| 1 | 비전 트리아지 | `pipeline/triage.py` | Claude (`VISION_MODEL`) | 사진 적합성 게이트 + 육안 소견 메타 추출 |
| 2 | 보고서 서술 생성 | `pipeline/report.py` | Claude → Solar → 목업 | 보고서 서술 3개 섹션 생성 |
| 3 | RAG 검색 임베딩 | `pipeline/rag.py`·`embedder.py` | Solar 임베딩(비대칭) | 근거 문서 top-k 검색 |
| 4 | (고도화) 멀티턴 대화 에이전트 | `v3_langgraph/`·`pipeline/multiturn.py` | Claude | 시설물명·위치 등 보고서 기본현황을 대화로 수집(기본 OFF) |
| 5 | (실험) ROI 2단계 탐지 | `test_roi_triage.py` | Claude Vision | 결함 관심영역(ROI) 좌표 추출 → 해당 영역만 YOLO(배경 오탐 감소). 파이프라인 미통합 |

LLM은 **서술·정보수집·영역제안**만 담당한다. 위험도 점수·등급은 Rule 엔진(코드), 근거 문장은 RAG(검색)에서 나오며, 프롬프트에 "재계산 금지·새로 지어내지 말 것"을 명시한다. 아래 1~3은 가동 중인 핵심 지점, 4·5는 고도화/실험(기본 OFF·미통합)이다.

---

## 1. 비전 트리아지 (Claude Vision)

**목적** — 무거운 YOLO 추론 전에 사진이 결함 근접분석에 적합한지 판정(게이트)하고, 동시에 보고서에 쓸 육안 소견 메타를 **한 번의 호출**로 함께 받는다(추가 호출 없음).

**호출 정보**

| 항목 | 값 |
|---|---|
| 파일 | `pipeline/triage.py` · `_vision_triage()` |
| 모델 | `config.VISION_MODEL` (미설정 시 `ANTHROPIC_MODEL` = `claude-sonnet-4-5`) |
| max_tokens | 400 |
| 입력 | 이미지(긴 변 `VISION_MAX_SIDE=1024`로 축소한 JPEG base64) + 아래 텍스트 프롬프트 |
| 출력 | JSON only (verdict + 메타) |
| 폴백 | 호출·파싱 실패 시 heuristic(라플라시안 흐림 점수)로 대체 → 앱 미중단 |

**프롬프트 전문**

```text
당신은 시설물 안전점검 보조 AI의 '1차 게이트'이자 '육안 소견' 담당입니다.
아래 사진이 콘크리트/강재 시설물의 '결함'을 자동 분석하기에 적합한지 판정하고,
사진에서 육안으로 보이는 결함과 정보를 메타데이터로 정리하세요.
과장·추측을 피하고 '실제로 보이는 것'만 적으세요. 정밀 위치·수치 판정은 하지 마세요.

대상 결함(6종): 균열 · 박리/박락 · 백태/누수 · 철근노출 · 강재손상 · 도장손상

반드시 아래 JSON 형식으로만 답하세요(코드블록·설명 없이 JSON만):
{
  "verdict": "ok | retake_far | retake_blur | not_crack",
  "message": "사용자에게 보여줄 한 줄 안내(한국어). ok면 빈 문자열",
  "structure_part": "벽|바닥|기둥|천장|외벽|계단|기타|미상",
  "material": "콘크리트|벽돌|타일|석재|강재|기타|미상",
  "orientation": "수직|수평|대각|불규칙|망상|미상",
  "branching": "없음|일부|많음|미상",
  "efflorescence": "있음|없음|미상",
  "spalling": "있음|없음|미상",
  "defects_observed": ["균열","철근노출"],
  "notes": "육안 소견 한 줄(예: 백태 동반 대각 균열, 철근노출 의심)"
}

- defects_observed: 위 6종 중 사진에서 '실제로 보이는' 결함만 한국어로 배열에 담으세요.
  확실치 않으면 넣지 마세요. 아무 결함도 안 보이면 빈 배열 [].

판정 기준(근접성 우선 — 이 도구는 '결함 근접 분석'용):
- retake_far : 건물 전체·외벽 전경처럼 구조물이 멀리/작게 찍혀 개별 결함의 폭·형태·경계를
               근접 판별할 수 없는 경우. (…생략: 매우 작게만 보이면 반드시 retake_far)
- retake_blur: 초점 흐림·흔들림으로 결함 경계가 뭉개짐
- not_crack  : 시설물 결함 점검 대상 사진이 아님(사람·풍경·문서 등)
- ok         : 벽면·기둥·바닥의 일부가 화면을 충분히 채운 근접~중거리 촬영. (애매하면 retake_far)
```

**후처리** — `_parse_vision()`이 응답에서 `{ … }` JSON 블록만 정규식으로 추출(코드펜스·잡텍스트 방어). 비전이 말한 한국어 결함명은 `_VISION_DEFECT_MAP`으로 정규 라벨(crack/spalling/…)로 매핑해 detector·Rule과 정합시킨다.

---

## 2. 보고서 서술 생성 (Claude → Solar → 목업)

**목적** — 6섹션 보고서 중 **서술이 필요한 3개 섹션**(2 점검결과 · 3 안전등급 · 4 종합의견)만 LLM이 작성한다. 1 기본현황·5 판단근거(RAG)·6 유의사항은 코드/검색 결과로 조립한다.

**호출 정보**

| 항목 | 값 |
|---|---|
| 파일 | `pipeline/report.py` · `_llm_narrative()`(Claude) / `_solar_narrative()`(Solar) |
| 제공자 체인 | `active_provider()` = claude → solar → mock (키·설정 따라 자동 선택) |
| Claude | `ANTHROPIC_MODEL` = `claude-sonnet-4-5`, max_tokens 1800 |
| Solar | `SOLAR_CHAT_MODEL` = `solar-pro2`, max_tokens 1800, temperature 0.3 (OpenAI 호환 chat/completions) |
| 입력 | `_prompt(feat, risk, rag, meta)` — 탐지 수치·복합결함·비전메타·**위험도(코드 산정)**·**RAG 근거**를 주입 |
| 출력 | 마크다운 `## 2./3./4.` 섹션 |

**프롬프트 템플릿** (실제 값이 `{ }`에 주입됨)

```text
당신은 시설물 안전점검 전문 AI입니다.

**목표:** 비전문가(건물주, 일반인)도 쉽게 이해할 수 있으면서, 정기안전점검 보고서
수준의 전문성을 갖춘 문서 작성
**어조:** {등급별 어조 가이드}   ← 정상/주의/위험/긴급별로 과장 없이 톤 조절

## 입력 데이터 (변경/재계산 금지, 그대로 인용)
**균열 탐지 결과** : 균열 개수 / 최고 신뢰도 / 길이(대각선 대비 %) / 폭(px, 상대값)
**복합 결함 (YOLO 탐지)** : {결함별 개소·신뢰도}
**AI 비전 육안 관찰 (트리아지)** : {부위·재질·방향·백태·박리·소견·육안 관찰 결함}
**위험도 평가 (코드 산정 — 재계산 금지)** : 점수 {score} / 등급 {grade} / 산정 근거
**안전기준 근거 (RAG 검색 — 새로 지어내지 말 것)** : {검색된 근거 문장 + 출처}

## 작성 규칙 (필수)
1. 데이터 정확성: 위 수치를 그대로 사용. 점수/등급 재계산 금지.
2. 균열폭 표현: mm 단위로 단정 금지. px는 상대값임을 명시. "0.3mm 기준"은 참고로만.
3. 결함 구분: YOLO 탐지 결함과 비전 관찰 결함 명확히 구분(비전 관찰은 "(육안)" 표시).
4. 과장 금지: "붕괴 위험"·"매우 심각" 등 과도한 표현 자제. 사실 기반 서술.
5. 전문가 권고: 반드시 "정밀점검 권고" 문구 포함.

## 출력 형식 (마크다운, 정확히 이 제목 사용)
## 2. 점검 결과   … 탐지 결과 표(개수·신뢰도·길이·폭), 복합결함 별도 표, 육안 표시
## 3. 안전등급 평가 … 점수·자가진단 등급·참고 등급 표 + 산정 근거 + 등급별 의미
## 4. 종합의견   … 3줄 이내 핵심(즉시조치/추가점검/유지관찰) + 권고 1~2개 + 정밀점검 권고
```

**환각 차단·강건성 설계**

- **점수·등급은 프롬프트에 이미 확정값으로 주입**되고 "재계산 금지"를 명시 → LLM이 위험도를 임의로 바꾸지 못함.
- **근거는 RAG 검색 결과만 인용**("새로 지어내지 말 것") → 없는 기준을 만들어내지 못함.
- `_parse_narrative()`로 `## 2./3./4.` 섹션만 분리, `_fill_empty()`가 LLM이 빠뜨린 섹션을 목업으로 보충.
- 비전↔탐지 교차검증 문단은 LLM이 누락해도 코드가 항상 첨부(데모 안전장치).
- 전 구간 폴백: Claude 실패 → Solar → 목업(템플릿). API가 없어도 보고서가 나온다.

---

## 3. RAG 검색 쿼리 + 임베딩 (Solar Embeddings)

**목적** — 탐지된 결함·위험 등급에서 검색 쿼리를 만들고, Solar 임베딩으로 ChromaDB에서 관련 **현행 안전기준 근거**를 top-k 검색한다. (LLM 생성이 아니라 검색이지만, 보고서 5섹션의 근거를 공급하는 핵심 스크립트라 함께 정리)

**호출 정보**

| 항목 | 값 |
|---|---|
| 파일 | `pipeline/rag.py` · `search()` / `embedder.py` |
| 임베딩(비대칭) | 문서=`solar-embedding-1-large-passage` · 쿼리=`solar-embedding-1-large-query` |
| 벡터 DB | ChromaDB (`knowledge/chroma`), 코사인, `RAG_TOP_K=3` |
| 매칭 임계값 | `RAG_MATCH_MIN_SCORE=0.30` (유사도 = 1 − 거리) |
| 폴백 | 인덱스 미구축 시 빈 결과 반환(앱 미중단), 임베딩 제공자 `bge` 로컬 대체 가능 |

**쿼리 생성 로직** (`_build_query`)

```text
기본:  "시설물 콘크리트 안전점검 기준"
+ 균열이 있으면        → 균열 도메인 키워드, 폭이 있으면 "균열 폭 0.3mm 허용 기준 조치"
+ 복합 결함 유형별     → 결함별 도메인 키워드(_DEFECT_QUERY[label])
+ 등급이 위험/긴급이면 → "정밀안전진단 긴급보수 사용제한"
```

**비대칭 임베딩 이유** — 인덱스 문서는 passage 모델로, 검색 쿼리는 query 모델로 임베딩해 **동일 벡터공간**에서 비교(Solar 권장 방식). 검색 결과는 `Evidence`(text·score·기준명·문서종류·발행처·URL·결함태그)로 반환되어 보고서·UI가 **파일명이 아니라 기준명·출처**로 인용한다.

**실측** — 7종 결함 대표 쿼리 스모크 테스트에서 모두 임계값(0.30) 위로 정확한 근거 검색(유사도 0.467~0.589).

---

## 4. 요약

LLM은 **판단하지 않고 서술만** 한다. 게이트·메타(트리아지 비전)와 서술 생성(보고서)만 LLM이 맡고, 점수는 Rule 코드가, 근거는 RAG 검색이 담당하며, 프롬프트마다 "재계산 금지·지어내지 말 것"을 명시하고 전 구간 폴백을 둔다 — 이것이 근거 없는 환각을 구조적으로 막는 본 시스템의 핵심 설계다.
