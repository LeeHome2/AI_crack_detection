# 위험도 산정 규칙 (Rule Engine) — AI 시설물 안전점검 시스템 (프로젝트 9번)


---

## 1. 산정 개요

탐지 결과와 형태 특징에서 **정량 가점을 합산**해 0~100점 점수를 만들고, **점수 구간으로 4등급**을 산정한다. 점수·등급은 **코드(Rule)가 결정론적으로 계산**하며 LLM은 재계산하지 않는다(설명·재현 가능). 각 가점은 기여 내역(rule·detail·points)으로 보고서에 기록된다.

```
총점 = 균열 채널 가점 + 비전 관찰 가점 + 면적 결함 가점 + 복합 가점   →  GRADE_BINS 구간으로 등급 결정
```

## 2. 등급 구간 (GRADE_BINS)

| 점수 구간 | 자가진단 등급 | 참고 상태평가등급 |
|---|---|---|
| 0 – 39 | 정상 | A~B등급 (양호) |
| 40 – 69 | 주의 | C등급 (보통) |
| 70 – 89 | 위험 | D등급 (미흡) |
| 90 이상 | 긴급 | E등급 (불량) |

## 3. 균열 채널 가점

균열은 신뢰도·개수·길이 세 축으로 가점한다(면적 결함 합산과 분리 → 중복 방지).

| 항목 | 조건 | 가점 |
|---|---|---|
| 탐지 신뢰도 | 최고 신뢰도 ≥ 0.10 (강한 탐지) | +30 |
| | 0.05 ≤ 최고 신뢰도 < 0.10 (약한 탐지) | +15 |
| 균열 개수 | 5개소 이상 | +25 |
| | 3 ~ 4개소 | +15 |
| 균열 길이 | 최장 균열 / 이미지 대각선 ≥ 15% | +15 |

(각 항목은 상호 배타적 구간에서 하나만 가산. 신뢰도·개수·길이 채널은 서로 독립적으로 합산.)

> **신뢰도 캘리브레이션** — 임계값은 본 모델의 실제 신뢰도 분포에 최적화해 계단식으로 설정했다(STRONG 0.10 · MODERATE 0.05). 결함별 신뢰도 하한(4절)과 형태·비전 검증(3.1·5절)을 함께 적용해 오탐을 억제한다.

### 3.1 비전 트리아지 관찰 기반 가점

YOLO 신뢰도가 매우 낮게만 잡힌 경우(< 0.05)에도 **AI 비전(Claude)이 육안으로 결함을 확인**하면 가점한다. 탐지와 비전 관찰을 교차해 실제 결함을 놓치지 않는 이중 안전장치.

| 조건 | 가점 |
|---|---|
| 비전이 균열 관찰 + YOLO 최고 신뢰도 < 0.05 | +20 |
| 비전이 균열 외 결함 관찰(유형당 +10) | 최대 +20 |

(이 가점이 적용되면 "탐지 신호 있음"으로 인정되어 RAG 긴급기준 가점 대상이 된다 → 모델이 아무것도 못 잡은 원거리·전경 사진이 근거만으로 점수를 받던 문제는 여전히 차단.)

## 4. 면적 결함 가점 (복합 결함, DEFECT_WEIGHTS)

균열 외 면적 결함은 **구조적 심각도 순**으로 기본 가점한다. 각 결함은 **결함별 신뢰도 하한(DEFECT_CONF_MIN)** 이상만 산정에 반영(약한 클래스는 하한을 높여 오탐 억제).

| 결함 | 기본 가점 | 신뢰도 하한 | 근거 |
|---|---|---|---|
| 철근노출 (rebar_exposure) | +30 | 0.25 | 피복 탈락·활성 부식·단면 손실 → 최고 위험 |
| 강재손상 (steel_defect) | +25 | 0.35 | 구조 강재 손상 |
| 박리/박락 (spalling) | +20 | 0.45 | 콘크리트 피복 손실(철근노출 전조) |
| 백태/누수 (efflorescence) | +10 | 0.35 | 수분 침투 지표(2차 열화 유발) |
| 도장손상 (paint_damage) | +5 | 0.45 | 방식 코팅·미관(경미) |
| 균열 (crack) | 0 | 0.20 | 균열 채널(3절)이 계산 → 중복 가점 방지 |

## 5. 복합 가점

| 조건 | 가점 | 취지 |
|---|---|---|
| 동일 결함 3개소 이상 | +5 | 반복 발생 심각도 소폭 반영 |
| 서로 다른 유의 결함 2종 이상 동시 | +10 | 복합 열화 상호작용(예: 균열+백태=누수성 균열, 박락+철근노출=진행성 열화) |

## 6. 산정 예시

> **예시 1** — 균열 3개소 탐지, 최고 신뢰도 0.22(≥0.10, 강), 최장 균열 길이 대각선 대비 16%
> = 강한 탐지(+30) + 균열 개수 3~4개소(+15) + 긴 균열 ≥15%(+15) = **60점 → 주의(C등급 수준)**
>
> **예시 2 (비전 보완)** — YOLO 최고 신뢰도 0.03(사실상 미탐)이지만 AI 비전이 균열 + 백태 관찰
> = 비전 균열 확인(+20) + 비전 복합결함 1종(+10) = **30점 → 정상**(단, "탐지 신호 있음"으로 인정)

## 7. 설계 원칙

점수는 **코드가 결정론적으로** 산정하고(같은 입력 → 같은 점수), 근거는 **RAG 검색**이 공급하며, 서술만 LLM이 담당한다. 이로써 위험도 판정이 **재현 가능하고 설명 가능**하며, LLM이 등급을 임의로 바꾸거나 지어내지 못한다. 가중치는 구조 안전 관점의 심각도 순으로 배치했고(철근노출 최고), 균열 채널과 면적 결함 채널을 분리해 중복 가점을 막았다. 결함별 신뢰도 하한은 클래스별 성능(AP50)을 프록시로 조정해, 강한 클래스는 확실히 탐지하고 약한 클래스는 오탐을 억제하도록 했다(실측 신뢰도 분포 확보 시 최종 확정).

> 본 규칙표는 코드(`pipeline/rules.py`)·설정(`config.py`)의 실제 상수와 일치하며, 값 변경 시 함께 갱신한다.
