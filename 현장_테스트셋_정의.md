# 현장 테스트셋 정의서 — 직접 촬영 이미지 전처리·활용

> AI 시설물 안전점검 시스템 (프로젝트 9번) · 1인 개발 이호민 · 최신화 7/28
> 대응 체크포인트 **CP4** — "AI HUB 데이터 외에 직접 촬영한 현장 이미지(100~300장)를 전처리하여 시연 및 테스트셋으로 적절히 활용했는가?"
> 〔작성자 확인〕 표시는 최종 제출 전 실제 값으로 채울 항목입니다.

---

## 1. 목적

공개 데이터셋(AIHub)만으로 학습한 모델이 **실제 현장·촬영 환경에서 어떻게 동작하는지**를 검증하기 위해, 개발자가 직접 촬영한 현장 이미지를 별도 테스트셋으로 구축했다. 이 테스트셋은 학습에 사용하지 않고, 오직 **시연과 도메인 검증**에만 사용한다(학습셋과의 분리).

- 학습 데이터: AIHub 공개 3종(162·567·71769) — "무엇을 탐지할지" 학습
- **현장 테스트셋: 직접 촬영 약 180장 — "실제 환경에서 되는지" 검증**

## 2. 촬영 개요

| 항목 | 내용 |
|---|---|
| 촬영 수량 | 약 **180장** (CP4 기준 100~300장 충족) |
| 촬영 기기 | 스마트폰 카메라 (모바일 실사용 시나리오와 동일) |
| 촬영 대상 | 〔작성자 확인〕 실제 건물·구조물 표면 (예: 아파트/주택 외벽, 옹벽, 주차장 기둥, 콘크리트 바닥 등) |
| 촬영 환경 | 〔작성자 확인〕 주간 자연광, 근접~중거리, 다양한 표면 재질(콘크리트·화강암 등) |
| 촬영 기간 | 〔작성자 확인〕 2026.07.〔  〕 ~ 07.〔  〕 |
| 결함 유형 | 균열 중심(선형·망상), 일부 박리·백태·도장손상 등 복합 결함 포함 |

> 스마트폰 근접 촬영이라, 드론 항공 시점(AIHub 162)이나 터널·댐(AIHub 71769)과 **도메인 차이**가 있다. 이 차이를 실측·확인하는 것이 본 테스트셋의 핵심 목적이다.

## 3. 전처리 파이프라인

현장 원본을 그대로 쓰지 않고 아래 3단계 전처리를 거쳐 테스트셋으로 정규화했다. 전처리는 저장소의 스크립트 **`tools/deid_blur.py`** 로 재현 가능하며, 처리 이력은 `manifest.csv`로 남긴다.

### 3.1 개인정보 비식별화 (필수)

현장 촬영 시 우발적으로 포함될 수 있는 **차량 번호판·사람 얼굴**을 자동 탐지해 블러 처리한다. 개인정보보호법상 식별정보를 산출물·발표자료로 노출하지 않기 위한 조치.

- 도구: `tools/deid_blur.py` (OpenCV 기반, 오프라인 동작)
- 처리: 얼굴·번호판 탐지 → 가우시안 블러(또는 모자이크)
- 검수: `_review/` 폴더의 박스 오버레이로 처리 대상 육안 확인, 놓친 컷은 수동 보정
- 증빙: `manifest.csv`에 이미지별 (해상도·얼굴수·번호판수·처리모드) 기록
- 오탐 억제: 번호판 후보를 가로세로비로 필터링해 **균열·텍스처를 개인정보로 오인·훼손하는 것을 방지**

### 3.2 EXIF 회전 보정

스마트폰 사진의 회전 메타(EXIF Orientation)를 실제 픽셀에 적용해, 분석 파이프라인 입력 시 방향이 뒤틀리지 않도록 정규화한다.

### 3.3 입력 규격 정규화

- 최대 변 길이 리사이즈(`--max-size`)로 과대 해상도 원본을 일정 규격으로 통일
- 분석 단계에서는 **640 타일 분할(overlap 0.2)** 로 미세·저대비 균열 신호를 보존(다운스케일 시 배경에 묻히는 문제 대응)

## 4. 활용 방식 (시연 + 도메인 검증)

본 테스트셋은 정량 라벨(GT) 기반 mAP 평가가 아니라, **실사용 시연과 정성적 도메인 검증**에 사용했다. CP4가 요구하는 "시연 및 테스트셋으로 적절히 활용"에 해당한다.

1. **실사용 시연**: 촬영 이미지를 앱에 업로드 → 탐지·특징추출·위험도·RAG 근거·보고서 자동생성 전 과정을 실제 현장 사진으로 시연.
2. **도메인갭 검증**: AIHub(터널·댐)로 학습한 seg 모델이 실제 외벽(화강암 등) 텍스처에서 오탐/미탐을 내는 현상을 확인.
3. **엔지니어링 의사결정의 근거**: 위 검증 결과가 **"균열 seg 단독 → bbox 2모델 하이브리드(crack 전용 + defect6)"** 채택 결정의 실증 근거가 됨. 즉 현장 데이터가 설계 판단에 직접 반영됨.

> 이 과정은 "남의 데이터 성능 자랑"이 아니라 **직접 촬영한 현장 데이터로 검증하고 그 결과로 설계를 바꾼** 데이터 기반 의사결정 사례다.

## 5. 대표 예시 (발표·산출물 첨부용)

〔작성자 확인〕 개인정보 없는(또는 블러 처리한) 대표 컷 4~6장을 아래 유형별로 선별해 첨부:

| 유형 | 설명 | 첨부 |
|---|---|---|
| 정상 탐지 | 균열이 정확히 탐지·정량화된 케이스 | 〔이미지〕 |
| 복합 결함 | 균열 + 박리/백태 등 동시 탐지 | 〔이미지〕 |
| 도메인갭(어려운 케이스) | 텍스처 오탐 또는 미탐이 관찰된 케이스 | 〔이미지〕 |
| 전처리 예시 | 비식별화(블러) 전/후 비교 | 〔이미지〕 |

## 6. 한계와 향후 계획

- **정량 GT 라벨 없음**: 시연·정성 검증 용도로, 박스/폴리라인 정답 라벨은 부여하지 않음(라벨링 공수·일정 제약). 정량 mAP 평가는 향후 과제.
- **학습 미사용**: 180장은 학습셋으로는 소규모라 과적합 위험이 있어 학습에 포함하지 않음.
- **향후**: 현장 이미지 + AIHub(71769 등)를 결합하고 hard negative를 추가해 **도메인 다양성을 확보한 재학습**으로 실환경 성능을 개선(개발계획 참조).

## 7. 체크포인트(CP4) 충족 근거 요약

| CP4 요구 | 충족 근거 |
|---|---|
| 직접 촬영 100~300장 | 약 180장 스마트폰 촬영 |
| 전처리 | 개인정보 비식별화(`deid_blur.py`) + EXIF 보정 + 규격 정규화, `manifest.csv` 증빙 |
| 시연 활용 | 앱 업로드 전 과정 실사진 시연 |
| 테스트셋 활용 | 도메인갭 검증 → 하이브리드 설계 결정의 실증 근거 |
